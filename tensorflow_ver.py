import tensorflow as tf
print("Tensor flow Version installed on your system")
if(tf.__version__):
    print(tf.__version__)
else:
    print("Tensor is Not installed.\n Please Install and Then check again!")